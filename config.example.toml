[analysis]
currency = "USD"
as_of = "YYYY-MM-DD"
minimum_sold_comps = 3
maximum_comp_age_days = 180
minimum_incremental_ev = 0.0
minimum_roi = 0.0

[comc]
# Atualizar a partir da tabela oficial vigente antes de cada análise.
standard_processing_fee = 0.0
select_processing_fee = 0.0
elite_processing_fee = 0.0
sale_fee_rate = 0.0
cashout_fee_rate = 0.0

[psa]
grading_fee = 0.0
inbound_shipping_per_card = 0.0
return_shipping_per_card = 0.0
insurance_per_card = 0.0

[cgc]
grading_fee = 0.0
inbound_shipping_per_card = 0.0
return_shipping_per_card = 0.0
insurance_per_card = 0.0
